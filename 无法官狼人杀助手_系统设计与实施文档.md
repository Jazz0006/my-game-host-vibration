# 无法官狼人杀助手：系统设计与实施文档

**文档版本：** v1.0  
**日期：** 2026-07-12  
**目标读者：** Codex / 开发者  
**项目阶段：** 最小可行产品（MVP）设计与实施

---

## 1. 项目背景

现有狼人杀微信小程序可以完成自动发牌、语音主持、玩家夜间操作和信息展示，从而避免安排一名玩家担任法官。

当前最大的体验问题是：夜间流程通常按照固定角色顺序播放语音并等待操作。为了避免闭眼玩家通过语音判断某个角色是否在场，即使某个角色不存在，系统也经常仍需播放对应提示或等待固定时长，导致夜间流程冗长。

本项目希望将夜间角色提醒从公共语音改为每位玩家手机上的私人提醒：

1. 房主手机只播放公共语音，例如“天黑请闭眼”和“天亮了”。
2. 所有玩家在黑夜阶段手持自己的手机。
3. 按照传统狼人杀的角色行动顺序，服务器逐个唤醒本局实际存在的角色。
4. 轮到某个角色行动时，该角色玩家的手机收到私人提醒：
   - Android 浏览器：震动 + 屏幕变化。
   - iPhone 浏览器：提示音 + 屏幕变化。
5. 玩家睁眼，在手机上完成选择，然后点击确认并闭眼。
6. 不存在的角色直接跳过，不产生等待。
7. 狼人阶段由所有狼人同时睁眼，面对面交流后，由任意一名狼人提交最终目标。

项目核心不是在线狼人杀，而是为线下面杀提供一个无需真人法官的流程引擎。

---

## 2. 核心目标

### 2.1 产品目标

实现一个手机浏览器即可使用的无法官狼人杀助手，支持：

- 房主创建房间。
- 玩家通过房间号或二维码加入。
- 自动分配座位。
- 自动发牌。
- 私密展示角色身份。
- 自动执行夜间角色顺序。
- 只唤醒实际存在的角色。
- 玩家手机震动或发出私人提示音。
- 玩家在手机上选择目标并确认。
- 房主查看流程进度和公开结果。
- 玩家或房主断线后恢复原房间、原座位和原身份。
- 服务器短暂重启后可恢复进行中的游戏。

### 2.2 非目标

MVP 阶段暂不实现：

- 完整的线上狼人杀聊天。
- 语音通话。
- 玩家不在同一地点的远程游戏。
- 复杂账号系统。
- 付费系统。
- 战绩排行。
- 多剧本编辑器。
- 微信小程序版本。

第一版以服务器网页版本为主，验证玩法和技术可靠性。后续可复用服务器和游戏引擎，开发微信小程序客户端。

---

## 3. 使用场景

### 3.1 创建房间

1. 房主打开网页。
2. 输入房主名称。
3. 点击“创建房间”。
4. 服务器生成 6 位房间号。
5. 房主页面显示：
   - 房间号。
   - 加入二维码。
   - 玩家列表。
   - 当前连接状态。

### 3.2 玩家加入

1. 玩家扫码或打开网页。
2. 输入房间号和名称。
3. 服务器分配座位号。
4. 玩家进入等待大厅。
5. 所有客户端实时更新玩家列表。

### 3.3 发牌

1. 房主设置人数和角色配置。
2. 房主点击“开始游戏”。
3. 服务器随机分配角色。
4. 每位玩家只能查看自己的身份。
5. 狼人可以在规定阶段看到狼队友。
6. 玩家确认已查看身份。

### 3.4 夜间流程

1. 房主播放“天黑请闭眼”。
2. 服务器按照规则引擎生成本夜实际行动队列。
3. 当前角色玩家收到私人提醒。
4. 玩家完成操作并提交。
5. 服务器校验并保存结果。
6. 自动进入下一个角色阶段。
7. 所有阶段结束后，房主收到“夜间已完成，可以宣布天亮”。
8. 房主播放“天亮了”，并公开需要公开的信息。

### 3.5 断线恢复

1. 玩家首次加入后，服务器生成专属 `resumeToken`。
2. 浏览器将房间号、玩家 ID 和恢复令牌保存在本地。
3. 短暂断网时 Socket.IO 自动重连。
4. 页面刷新或浏览器关闭后，重新打开网页可一键恢复。
5. 服务器重新发送该玩家当前完整视图状态。
6. 如果更换设备，可由房主生成一次性恢复码。
7. 新设备恢复成功后，旧设备连接立即失效。

---

## 4. 技术架构

### 4.1 总体架构

```text
玩家手机浏览器 ─┐
玩家手机浏览器 ─┼── HTTPS / WSS ── Nginx ── Node.js + Socket.IO
房主手机浏览器 ─┘                         │
                                          ├── 游戏状态机
                                          ├── 房间管理
                                          ├── 会话恢复
                                          ├── SQLite
                                          └── 事件日志
```

### 4.2 部署环境

- 云平台：Oracle Cloud
- 实例：VM.Standard.E2.1.Micro
- 操作系统：Ubuntu 24.04
- 反向代理：Nginx
- 后端：Node.js + TypeScript
- 实时通信：Socket.IO
- 数据库：SQLite
- 进程管理：PM2
- HTTPS：Let's Encrypt / Certbot
- 源代码管理：GitHub

### 4.3 开发环境

开发不直接在 Oracle VM 上完成。

推荐流程：

```text
Windows 本地开发
    ↓
本地多浏览器与机器人测试
    ↓
Git commit / push
    ↓
Oracle VM git pull
    ↓
PM2 重启应用
```

---

## 5. 推荐技术栈

### 后端

- Node.js
- TypeScript
- Express
- Socket.IO
- SQLite
- Vitest
- Zod（可选，用于事件参数校验）
- nanoid 或 Node.js `crypto`（生成令牌）

### 前端

MVP 推荐使用：

- 原生 HTML
- CSS
- JavaScript
- Socket.IO Client

暂不引入 React 或 Vue。先验证房间、状态机、恢复机制和夜间交互。界面复杂度提高后再考虑前端框架。

### 运维

- Nginx
- PM2
- Git
- Certbot
- systemd / cron 备份

---

## 6. 项目目录建议

```text
werewolf-host/
├── src/
│   ├── server.ts
│   ├── config/
│   │   └── env.ts
│   ├── domain/
│   │   ├── room.ts
│   │   ├── player.ts
│   │   ├── game.ts
│   │   ├── role.ts
│   │   └── action.ts
│   ├── services/
│   │   ├── room-service.ts
│   │   ├── session-service.ts
│   │   ├── game-service.ts
│   │   ├── night-engine.ts
│   │   └── recovery-service.ts
│   ├── socket/
│   │   ├── index.ts
│   │   ├── host-handlers.ts
│   │   ├── player-handlers.ts
│   │   └── middleware.ts
│   ├── persistence/
│   │   ├── database.ts
│   │   ├── room-repository.ts
│   │   ├── player-repository.ts
│   │   └── event-repository.ts
│   └── utils/
│       ├── token.ts
│       ├── room-id.ts
│       └── logger.ts
├── public/
│   ├── index.html
│   ├── host.html
│   ├── player.html
│   ├── css/
│   ├── js/
│   ├── audio/
│   └── images/
├── tests/
│   ├── room.test.ts
│   ├── recovery.test.ts
│   ├── night-engine.test.ts
│   └── integration/
├── data/
│   └── werewolf.sqlite
├── scripts/
│   ├── test-bots.ts
│   └── migrate.ts
├── package.json
├── tsconfig.json
├── .env.example
├── .gitignore
└── README.md
```

---

## 7. 核心数据模型

### 7.1 Room

```ts
type RoomStatus =
  | "lobby"
  | "role_reveal"
  | "night"
  | "day"
  | "finished";

type Room = {
  id: string;
  hostPlayerId: string;
  status: RoomStatus;
  dayNumber: number;
  players: Player[];
  gameConfig: GameConfig | null;
  gameState: GameState | null;
  createdAt: number;
  updatedAt: number;
  expiresAt: number;
};
```

### 7.2 Player

```ts
type PlayerControllerType = "human" | "ai";

type Player = {
  id: string;
  roomId: string;
  name: string;
  seat: number;
  isHost: boolean;
  controllerType: PlayerControllerType;
  connected: boolean;
  currentSocketId: string | null;

  resumeTokenHash: string;
  manualRecoveryCodeHash: string | null;

  roleId: string | null;
  alive: boolean;
  ready: boolean;
  roleViewed: boolean;

  createdAt: number;
  lastSeenAt: number;
};
```

注意：

- 不在数据库中明文保存 `resumeToken`。
- 保存令牌哈希。
- `socket.id` 只是临时连接标识，不是玩家身份。

### 7.3 GameConfig

```ts
type GameConfig = {
  playerCount: number;
  roles: Array<{
    roleId: string;
    count: number;
  }>;
  rulesetId: string;
  rulesetVersion: number;
  maxRoomPlayers: number;
  enableBackgroundMusic: boolean;
  enableVibration: boolean;
};
```

`playerCount` 和角色数量不得在状态机中写死。所谓“人数不限”是指领域引擎支持动态人数和动态角色数量；实际部署仍应通过 `maxRoomPlayers` 设置合理的单房间容量上限。

当前基础目标中的所有玩家均为：

```ts
controllerType: "human"
```

`controllerType: "ai"` 只作为未来扩展接口保留，当前阶段不实现模型、API Key、Prompt 或 AI 决策逻辑。

### 7.4 GameState

```ts
type GamePhase =
  | "waiting"
  | "role_reveal"
  | "night_start"
  | "night_action"
  | "night_complete"
  | "day_discussion"
  | "voting"
  | "game_over";

type GameState = {
  phase: GamePhase;
  dayNumber: number;
  nightQueue: NightStep[];
  currentNightStepIndex: number;
  pendingPublicResults: PublicResult[];
  winner: "good" | "evil" | null;
  revision: number;
};
```

### 7.5 NightStep

```ts
type NightStepStatus =
  | "pending"
  | "active"
  | "completed"
  | "skipped";

type NightStep = {
  id: string;
  roleId: string;
  actingPlayerIds: string[];
  status: NightStepStatus;
  startedAt: number | null;
  completedAt: number | null;
  submittedByPlayerId: string | null;
  result: unknown;
};
```

狼人阶段中：

```ts
actingPlayerIds = 所有存活狼人玩家 ID
```

任意一个狼人提交后，该步骤完成。

---

## 8. 客户端视图模型

服务器不能把完整房间状态发送给所有玩家，否则会泄露身份。

必须针对每个连接生成独立视图：

```ts
type PlayerView = {
  room: {
    id: string;
    status: RoomStatus;
    dayNumber: number;
  };

  self: {
    playerId: string;
    name: string;
    seat: number;
    alive: boolean;
    roleId?: string;
  };

  players: Array<{
    playerId: string;
    name: string;
    seat: number;
    connected: boolean;
    alive?: boolean;
  }>;

  action:
    | {
        status: "waiting";
      }
    | {
        status: "active";
        actionId: string;
        roleId: string;
        prompt: string;
        validTargets: number[];
      }
    | {
        status: "completed";
      };

  serverRevision: number;
};
```

核心原则：

> 客户端不拥有真实游戏状态，服务器是唯一真相来源。

每次恢复连接后，服务器发送完整 `PlayerView`，而不是只补发某个事件。

---

## 9. Socket.IO 事件设计

### 9.1 通用规则

- 所有客户端请求都使用 acknowledgment 返回成功或失败。
- 所有写操作带唯一 `requestId`，避免重复提交。
- 服务器校验权限、房间状态和参数。
- 客户端不能自行决定流程进入下一阶段。
- 服务器发送事件时尽量包含 `serverRevision`。

### 9.2 房主事件

#### `host:create-room`

请求：

```ts
{
  requestId: string;
  name: string;
}
```

响应：

```ts
{
  ok: true;
  roomId: string;
  playerId: string;
  resumeToken: string;
}
```

#### `host:start-game`

```ts
{
  requestId: string;
  roomId: string;
  config: GameConfig;
}
```

#### `host:start-night`

```ts
{
  requestId: string;
  roomId: string;
}
```

#### `host:advance-to-day`

只允许夜间完成后调用。

#### `host:issue-recovery-code`

```ts
{
  requestId: string;
  targetPlayerId: string;
}
```

### 9.3 玩家事件

#### `player:join-room`

```ts
{
  requestId: string;
  roomId: string;
  name: string;
}
```

#### `player:resume`

```ts
{
  requestId: string;
  roomId: string;
  playerId: string;
  resumeToken: string;
}
```

#### `player:manual-recover`

```ts
{
  requestId: string;
  roomId: string;
  seat: number;
  recoveryCode: string;
}
```

#### `player:confirm-role-viewed`

#### `player:submit-action`

```ts
{
  requestId: string;
  actionId: string;
  targetSeat?: number;
  choice?: string;
}
```

### 9.4 服务器推送事件

- `room:state`
- `player:view`
- `player:replaced`
- `connection:warning`
- `game:role-assigned`
- `game:night-started`
- `game:action-activated`
- `game:action-completed`
- `game:night-complete`
- `game:day-started`
- `game:ended`
- `server:error`

---

## 10. 断线恢复设计

断线恢复是本项目最高优先级功能。

### 10.1 三层恢复机制

#### 第一层：Socket.IO 自动重连

适用于：

- Wi-Fi 短暂波动。
- 手机网络瞬时中断。
- WebSocket 临时断开。

界面显示：

```text
网络连接中……
```

恢复后服务器重新推送完整玩家视图。

#### 第二层：本地恢复凭证

首次创建或加入房间时，服务器返回：

```ts
{
  roomId,
  playerId,
  resumeToken
}
```

浏览器保存：

```js
localStorage.setItem(
  "werewolfSession",
  JSON.stringify({
    roomId,
    playerId,
    resumeToken
  })
);
```

重新打开网页后：

1. 读取本地会话。
2. 调用 `player:resume`。
3. 服务器验证令牌。
4. 更新玩家的 `currentSocketId`。
5. 踢掉旧连接。
6. 返回最新 `PlayerView`。

#### 第三层：人工恢复码

适用于：

- 换手机。
- 清除浏览器数据。
- 无痕模式。
- 本地存储丢失。

流程：

1. 房主在管理界面选择玩家。
2. 生成短期一次性恢复码。
3. 玩家输入房间号、座位号、恢复码。
4. 服务器签发新的 `resumeToken`。
5. 原恢复令牌立即失效。

### 10.2 恢复后的界面规则

- 尚未轮到该玩家：显示“等待行动，请闭眼”。
- 正在轮到该玩家：重新显示当前操作，并再次提醒。
- 已提交：显示“已完成，请闭眼等待”。
- 游戏已进入下一阶段：直接显示新阶段状态。
- 玩家身份已在其他设备恢复：旧设备显示“你的身份已在另一台设备恢复”。

### 10.3 幂等设计

玩家可能在提交操作后、收到服务器确认前断线。

因此 `player:submit-action` 必须带 `requestId`：

```ts
{
  requestId: "uuid",
  actionId: "night-step-id",
  targetSeat: 5
}
```

服务器保存已经处理过的 `requestId`。

重复收到同一请求时，不重复执行，只返回第一次结果。

---

## 11. 夜间状态机

### 11.1 设计原则

- 严格按照传统角色顺序执行。
- 不并行执行不同角色。
- 本局不存在的角色直接跳过。
- 死亡或失去技能的角色按规则判断是否跳过。
- 服务器控制流程，客户端只提交选择。
- 任何阶段都可以通过服务器状态恢复。

### 11.2 队列生成

每晚根据仍然存活的实际角色生成行动队列：

```ts
const nightOrder = [
  "guard",
  "werewolf",
  "witch",
  "seer"
];
```

女巫双药均已耗尽时不再进入女巫阶段，也不发送行动提醒。若女巫只剩解药而狼人本晚空刀，同样直接跳过女巫阶段。

猎人夜间被狼刀后，天亮先公布死亡名单，再由猎人决定开枪或不开枪；被女巫毒死的猎人不能开枪。

实际顺序应由规则配置文件定义，不要硬编码在 Socket 处理函数中。

生成队列时：

1. 读取当前夜晚类型。
2. 遍历规则顺序。
3. 判断角色是否在场。
4. 判断角色是否满足行动条件。
5. 创建 `NightStep`。
6. 过滤或标记跳过步骤。

### 11.3 狼人阶段

- 所有存活狼人同时收到唤醒提醒。
- 狼人线下面对面商量。
- 任意一个狼人提交目标。
- 服务端校验提交者属于当前狼人行动组。
- 第一次有效提交完成该阶段。
- 其他狼人立即收到“本阶段已完成”。

可选增强：

- 提交前显示二次确认。
- 允许在尚未完成前修改选择。
- 房主可配置“任意狼人提交”或“指定狼王提交”。

### 11.4 超时处理

MVP 不建议自动跳过真人行动。

房主端显示：

```text
当前：预言家阶段
已等待：32 秒
玩家连接：正常
```

并提供：

- 再次提醒。
- 跳过该角色。
- 取消夜间。
- 强制完成。

所有强制操作必须写入事件日志。

---

## 12. 手机提醒设计

### 12.1 Android

优先使用：

```js
navigator.vibrate([200, 100, 200]);
```

同时：

- 屏幕切换到操作界面。
- 播放轻提示音。
- 显示醒目但不会被旁人看到的提示。

### 12.2 iPhone

Safari 不提供可靠的网页震动 API，因此使用：

- 轻提示音。
- 背景音乐短暂变化。
- 屏幕切换。
- 玩家始终手持手机且网页保持前台。

### 12.3 音频权限

手机浏览器通常要求用户先交互才能播放音频。

加入游戏后提供明确按钮：

```text
进入游戏并启用声音
```

点击后：

- 初始化 AudioContext。
- 播放一次极短的静音或测试音。
- 缓存音频资源。
- 检查提示音和背景音乐是否可用。

### 12.4 夜间使用要求

游戏开始前提示所有玩家：

- 手机保持解锁。
- 网页保持前台。
- 不切换其他应用。
- 全程手持手机。
- 系统音量保持在约定水平。
- 可开启低音量背景音乐掩盖震动和动作声。

---

## 13. 数据持久化

### 13.1 MVP 初期

最初可使用内存保存大厅，快速验证：

```ts
const rooms = new Map<string, Room>();
```

但在开发断线恢复和正式测试前，应迁移至 SQLite。

### 13.2 SQLite 表建议

```sql
CREATE TABLE rooms (
  id TEXT PRIMARY KEY,
  host_player_id TEXT NOT NULL,
  status TEXT NOT NULL,
  day_number INTEGER NOT NULL,
  game_config_json TEXT,
  game_state_json TEXT,
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL,
  expires_at INTEGER NOT NULL
);

CREATE TABLE players (
  id TEXT PRIMARY KEY,
  room_id TEXT NOT NULL,
  name TEXT NOT NULL,
  seat INTEGER NOT NULL,
  is_host INTEGER NOT NULL,
  connected INTEGER NOT NULL,
  resume_token_hash TEXT NOT NULL,
  manual_recovery_code_hash TEXT,
  role_id TEXT,
  alive INTEGER NOT NULL,
  ready INTEGER NOT NULL,
  role_viewed INTEGER NOT NULL,
  created_at INTEGER NOT NULL,
  last_seen_at INTEGER NOT NULL
);

CREATE TABLE game_events (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  room_id TEXT NOT NULL,
  event_type TEXT NOT NULL,
  actor_player_id TEXT,
  payload_json TEXT,
  created_at INTEGER NOT NULL
);

CREATE TABLE processed_requests (
  request_id TEXT PRIMARY KEY,
  room_id TEXT NOT NULL,
  player_id TEXT,
  response_json TEXT NOT NULL,
  created_at INTEGER NOT NULL
);
```

### 13.3 保存时机

至少在以下事件后持久化：

- 房间创建。
- 玩家加入。
- 玩家恢复。
- 发牌完成。
- 游戏阶段变化。
- 夜间步骤开始。
- 玩家提交操作。
- 夜间结算完成。
- 白天开始。
- 玩家死亡。
- 游戏结束。

---

## 14. 安全要求

### 14.1 身份隔离

- 任何玩家不能获取其他玩家角色。
- 房主客户端也不应默认获取全部身份，除非产品明确需要。
- 服务端按玩家生成视图。
- 不在前端 HTML 或 JavaScript 中预加载完整角色分配。

### 14.2 恢复令牌

- 使用高强度随机值。
- 数据库只保存哈希。
- 新设备恢复后令牌轮换。
- 房间结束后失效。
- 不在日志中输出明文令牌。

### 14.3 连接安全

生产环境必须使用：

- HTTPS
- WSS
- Nginx
- 合理的速率限制
- 请求参数校验

### 14.4 防止重复身份

同一玩家只允许一个活动控制连接。

新连接恢复成功后：

1. 标记旧 Socket 失效。
2. 向旧 Socket 发送 `player:replaced`。
3. 断开旧连接。
4. 更新数据库中的 `currentSocketId`。

---

## 15. 日志和可观察性

开发阶段必须记录：

```text
20:15:02 房间 582731 创建
20:15:25 玩家 7 加入
20:17:10 发牌完成
20:20:03 狼人阶段开始
20:20:18 玩家 3 提交击杀 5 号
20:20:19 狼人阶段完成
20:20:20 女巫阶段开始
```

建议日志字段：

```ts
{
  timestamp,
  roomId,
  playerId,
  socketId,
  eventType,
  requestId,
  status,
  details
}
```

注意不要记录明文恢复令牌。

房主开发模式可提供调试面板：

- 当前房间状态。
- 当前阶段。
- 玩家在线状态。
- 当前行动者。
- 最近事件。
- 手动模拟断线。
- 手动重新提醒。
- 强制跳过阶段。

---

## 16. 测试策略

### 16.1 本地多客户端测试

使用：

- Chrome 普通窗口：房主。
- Chrome 无痕窗口：玩家 1。
- Edge：玩家 2。
- Firefox：玩家 3。
- Chrome 多 Profile：更多玩家。

### 16.2 机器人玩家

创建 `scripts/test-bots.ts`：

```bash
npm run bots -- --count 10
```

机器人可模拟：

- 加入。
- 准备。
- 查看身份。
- 自动选择目标。
- 延迟提交。
- 超时。
- 断线。
- 重连。
- 重复提交。
- 错误令牌恢复。

### 16.3 必测断线场景

1. 玩家等待时断线。
2. 玩家正在行动时断线。
3. 提交后、服务器确认前断线。
4. 房主夜间断线。
5. 同一玩家从第二台设备恢复。
6. 旧设备重新上线。
7. 浏览器刷新。
8. 浏览器关闭后重新进入。
9. 服务器进程重启。
10. SQLite 恢复房间。
11. WebSocket 降级为轮询。
12. Wi-Fi 与移动网络切换。

### 16.4 真机测试

至少使用：

- 1 台 Android。
- 1 台 iPhone。
- 电脑上的多个模拟客户端。

真机重点验证：

- 震动。
- 音频权限。
- Safari 行为。
- 页面前后台。
- 锁屏。
- 二维码。
- 移动网络。
- 背景音乐。
- 横竖屏与不同屏幕尺寸。

---

## 17. 开发里程碑

### 里程碑 1：通信骨架

目标：

- Express 正常运行。
- `/health` 可访问。
- Socket.IO 客户端可连接。
- 网页显示连接状态。

验收：

```text
http://localhost:3000/health
```

返回：

```json
{
  "ok": true,
  "time": "..."
}
```

### 里程碑 2：大厅与房间

目标：

- 房主创建房间。
- 生成 6 位房间号。
- 玩家加入。
- 自动分配座位。
- 所有客户端实时看到玩家列表。
- 断线玩家显示离线。

### 里程碑 3：可靠恢复

目标：

- `resumeToken`。
- 页面刷新恢复。
- 关闭浏览器恢复。
- 房主恢复。
- 新设备恢复。
- 旧设备失效。
- 完整 `PlayerView` 同步。

这是 MVP 最重要的里程碑。

### 里程碑 4：测试行动

目标：

- 房主选择一名测试玩家。
- 指定玩家收到私人提醒。
- Android 震动。
- iPhone 提示音。
- 玩家提交完成。
- 房主看到结果。
- 重复提交不会重复执行。

### 里程碑 5：自动发牌

目标：

- 设置人数和角色。
- 随机发牌。
- 私密查看身份。
- 确认查看。
- 狼人队友信息。

### 里程碑 6：夜间状态机

目标：

- 生成夜间队列。
- 跳过不存在角色。
- 严格按顺序执行。
- 狼人任意一人提交。
- 当前角色完成后自动下一步。
- 夜间结束提示房主。

### 里程碑 7：SQLite 持久化

目标：

- 房间写入数据库。
- 玩家会话写入数据库。
- 游戏状态保存。
- 服务器重启恢复。
- 事件日志。
- 请求幂等。

### 里程碑 8：Oracle 部署

目标：

- GitHub 部署。
- PM2 运行。
- Nginx 反向代理。
- HTTPS。
- WSS。
- 域名。
- 自动备份。

### 里程碑 9：真实游戏测试

目标：

- 6–12 人实际面杀。
- 收集夜间时长。
- 记录断线次数。
- 观察震动是否暴露。
- 优化提示音与背景音乐。
- 修复交互问题。

---

## 18. 当前已完成状态

截至 2026-07-30，狼人杀模式已经形成可以作为后续多游戏扩展基线的完整版本。

目前已经完成：

- 本地 Node.js + TypeScript、Express、Socket.IO 技术骨架。
- `/health` 健康检查和开发环境对局实验室。
- 房主创建房间、玩家加入、在线状态和连续座位号。
- 玩家姓名本地保存、默认姓名分配、重名校验和房内改名。
- 圆桌/长桌座位展示，房主可在开局前拖动调整座位顺序。
- `resumeToken` 自动恢复原房间、座位、玩家身份和房主权限。
- 同一身份的新连接替换旧连接。
- 房主移除玩家、转让房主、转让后退出和关闭整个房间。
- 玩家在开局前主动退出房间。
- 5–12 人预设身份配置和自定义合法身份牌组。
- 狼人、预言家、女巫、守卫、猎人、平民六种身份。
- 私密身份分配、身份确认和随时重新查看自身身份。
- 守卫、狼人、女巫、预言家夜间行动队列。
- 可选夜间行动、女巫药品次数、守卫连续保护限制。
- 猎人夜间死亡、白天放逐和被毒不能开枪的结算。
- 白天全员投票、PK 投票、平票无人出局和自动计票。
- 玩家死亡、胜负判断、昼夜多轮循环和重新开局。
- 等待界面、私人震动提醒、天亮公告和游戏结果界面。
- `actionId` 校验、重复提交保护和行动中断线恢复。
- 单元测试、Socket.IO 多客户端测试和本地通信闭环验证。

阶段 0 基线确认：

- 提交 `0473107` 已包含最新房间、座位、配置和前端交互修改。
- 工作树在该提交后保持干净。
- `npm run typecheck` 通过。
- `npm test` 共 71 项测试全部通过。
- 启动本地服务器后，`npm run verify:flow` 通信闭环验证通过。

多游戏与持久化改造后，当前实现状态如下：

1. 多游戏引擎注册表、独立人数规则和私人视图边界已经建立。
2. 狼人杀正式开放；斗地主规则、三客户端通信和手机端 UI 已实现，并开放“测试版”创建入口；血染钟楼仅保留入口。
3. 斗地主命令已经使用统一 `requestId`、`stateRevision` 和服务端玩家身份绑定。
4. 房间、恢复令牌哈希和游戏联合状态已写入 SQLite，服务器重启后可恢复。
5. 游戏及房间事件使用单调递增 `sequence` 写入脱敏日志，并设置单房间数量上限。
6. `src/server.ts` 仍承担较多 Socket 流程编排，后续接入血染钟楼前应继续拆分应用服务。
7. iPhone 提示音、生产 HTTPS/WSS、正式部署和三台真机验收尚未完成。

---

## 19. Codex 实施要求

Codex 在继续开发时应遵守：

1. 一次只完成一个里程碑。
2. 每次修改后列出：
   - 修改了哪些文件。
   - 实现了什么。
   - 如何运行。
   - 如何手工测试。
   - 下一步建议。
3. 不要在大厅和恢复机制稳定前实现角色规则。
4. 不要把 `socket.id` 当作玩家身份。
5. 所有 Socket 写操作必须使用 acknowledgment。
6. 所有提交操作必须支持幂等。
7. 服务器必须是唯一状态源。
8. 前端只能收到当前玩家有权查看的数据。
9. 断线恢复优先级高于界面美化。
10. 所有核心游戏逻辑应独立于 Socket 处理函数。
11. 为房间、恢复和夜间状态机编写单元测试。
12. 所有敏感令牌不得写入日志。
13. Android 与 iPhone 提醒能力必须分开处理。
14. 生产环境必须使用 HTTPS/WSS。
15. Oracle VM 只作为部署环境，本地 Windows 用于日常开发。
16. 当前产品优先完成全真人线下面杀，不提前实现 AI 业务逻辑。
17. 人数、角色数量和夜间行动者数量不得硬编码在 Socket 处理函数中。
18. 房主权限、玩家存活状态、游戏身份和控制器类型必须相互独立。
19. 领域引擎不得根据真人或 AI 分支执行规则；所有控制器提交相同的玩家命令。
20. 传输事件、玩家命令、领域事件和玩家视图必须使用不同类型表达。
21. 房间管理规则与具体游戏规则必须分离；玩家数量限制由选中的游戏引擎决定。
22. 新游戏必须通过统一引擎注册表接入，不得继续向 `server.ts` 堆叠具体角色或牌型分支。
23. 每个游戏必须独立实现状态、命令、规则、私人视图和测试，不得共享包含隐藏信息的内部状态。
24. 多游戏公共前端只负责入口、房间和通用组件，具体对局界面按游戏模块加载。

---

## 20. 下一步明确任务

下一次开发应完成“从狼人杀专用服务重构为可注册的多游戏引擎平台”。本里程碑只调整架构边界，不同时实现斗地主规则。

### 20.1 目标

- 建立 `GameKind`、`GameEngine`、`GameInstance` 和引擎注册表。
- 将现有狼人杀状态与规则迁入独立 `games/werewolf` 模块。
- 将私人玩家视图和当前行动者计算从 `server.ts` 移出。
- 将房间最小/最大人数改为由游戏引擎元数据决定。
- 创建房间或进入配置前选择游戏类型。
- 保持房主只是房间管理员；游戏权限由引擎根据玩家和状态判断。
- 为斗地主预留固定 3 人、手牌私密视图和回合制出牌命令。
- 为血染钟楼预留自动说书人内部策略和不同可见范围的领域事件。

### 20.2 验收要求

1. 现有狼人杀 71 项测试继续全部通过。
2. 狼人杀页面和 Socket 行为保持兼容。
3. `server.ts` 不再直接判断狼人、女巫、预言家等具体角色。
4. 注册表能够返回狼人杀、斗地主和血染钟楼的游戏元数据；后两者可以先标记为未开放。
5. 房间人数校验通过所选引擎执行，斗地主元数据可以表达恰好 3 人。
6. 公共房间状态和私人游戏状态保持分离。
7. 未注册的游戏类型、错误游戏命令和跨游戏命令由服务端拒绝。
8. 本里程碑不实现模型调用、API Key 或 AI 玩家。

完成后再依次推进：

1. 接入标准三人斗地主完整规则和多人对局界面。
2. 为斗地主补齐断线恢复、隐私测试和机器人机械流程模拟。
3. 建立 SQLite 持久化、正式事件日志和进程重启恢复。
4. 接入《暗流涌动》血染钟楼状态与自动说书人算法。
5. 完成血染钟楼首夜、后续夜晚、白天提名投票和胜负闭环。

详细任务拆分、目录方案和验收标准见《多游戏平台下一阶段实施工作文档.md》。

---

## 21. 产品优先级与长期边界

### 21.1 当前基础目标

首先打造一个稳定、通用、可扩展的全真人线下桌游主持平台：

- 所有参与者都是真人。
- 玩家在线下自由发言和交流。
- 手机负责游戏入口、房间、私密信息、玩家行动、流程提示和结果结算。
- 第一款稳定游戏是狼人杀，下一款游戏是标准三人斗地主。
- 血染钟楼先保留入口和架构位置，待斗地主稳定后再接入自动说书人。
- 每种游戏独立定义人数、配置、行动规则和私人视图。
- 服务端是唯一游戏事实来源。
- 公共房间能力和游戏引擎能力相互独立。
- 优先完成多游戏引擎、斗地主、恢复、持久化和真机验证。

领域层的“人数不限”不代表生产环境允许无限连接。实际服务必须设置合理的：

- 单房间最大人数。
- 同时房间数量。
- 创建和加入速率限制。
- 空房间清理时间。
- 事件与快照保留上限。

### 21.2 未来混合真人与 AI 场景

基础目标完成后，平台需要支持：

- 真人数量不足时由房主补充一个或多个 AI 玩家，例如 5 名真人补 3 名 AI 组成 8 人局。
- 只有一名真人、其余全部为 AI 的对局。
- 主持系统运行在服务器上，由服务器使用安全保存的 API Key 调用模型服务。

`werewolf-ai` 项目中的主持状态机是早期实验用途，不作为合并后的权威状态机。未来只提取和复用其中的：

- AI 决策逻辑。
- 玩家记忆与信念系统。
- Prompt 构造。
- 结构化输出解析和校验。
- 模型调用、重试、追踪和保底机制。

最终权威状态机始终属于本项目。

### 21.3 当前阶段明确不做

- 不调用任何模型 API。
- 不接入 API Key。
- 不合并 `werewolf-ai` 的临时游戏状态机。
- 不为 AI 单独复制一套游戏规则。
- 不为了 AI 场景推迟全真人核心流程。

---

## 22. 为未来 AI 保留的最小接口

### 22.1 玩家控制器

房主权限、连接状态、存活状态、角色和控制器类型必须分开：

```ts
type PlayerControllerType = "human" | "ai";

type Player = {
  id: string;
  seat: number;
  name: string;
  isHost: boolean;
  controllerType: PlayerControllerType;
  connected: boolean;
  alive: boolean;
  roleId: string | null;
};
```

AI 玩家默认不能成为房主。房主即使在游戏中死亡，也仍保留房间管理权限。

### 22.2 统一玩家命令

真人和未来 AI 必须提交完全相同的命令：

```ts
type PlayerCommand<TType extends string, TPayload> = {
  requestId: string;
  gameId: string;
  playerId: string;
  actionId: string;
  stateRevision: number;
  type: TType;
  payload: TPayload;
};
```

真人命令通过 Socket.IO 传输；未来 AI 命令通过服务器内部控制器提交。领域状态机不得根据来源绕过权限或规则校验。

### 22.3 统一私人玩家视图

```ts
type PlayerView = {
  gameId: string;
  playerId: string;
  phase: GamePhase;
  stateRevision: number;
  self: {
    seat: number;
    roleId: string | null;
    alive: boolean;
  };
  players: PublicPlayerView[];
  legalActions: LegalAction[];
  visibleEvents: GameEvent[];
};
```

未来 AI 只能收到与其真人客户端同等权限的 `PlayerView`，不得接收完整身份表或服务端内部状态。

---

## 23. 命令、领域事件与传输事件

必须明确区分三种概念：

1. `PlayerCommand`：玩家请求执行操作，例如提交狼人目标。
2. `GameEvent`：服务端校验后确认已经发生的领域事实。
3. Socket.IO 事件：命令和视图的传输方式，不是游戏事实本身。

推荐领域事件 envelope：

```ts
type GameEvent<TType extends string, TPayload> = {
  schemaVersion: number;
  eventId: string;
  gameId: string;
  sequence: number;
  type: TType;
  round: number;
  phase: GamePhase;
  actorId: string | null;
  visibility: "public" | string[];
  causationId: string | null;
  correlationId: string | null;
  occurredAt: number;
  payload: TPayload;
};
```

规则判断必须依赖结构化 `payload`，不能解析展示文本。每局事件必须有单调递增的 `sequence`，以支持断线补发、持久化恢复和未来 AI 记忆按顺序消费。

建议优先定义的事件包括：

- `game_started`
- `role_assigned`
- `role_confirmed`
- `phase_started`
- `action_requested`
- `action_submitted`
- `wolf_target_selected`
- `witch_action_resolved`
- `seer_check_resolved`
- `player_eliminated`
- `speech_turn_started`
- `speech_published`
- `vote_submitted`
- `vote_resolved`
- `game_completed`

---

## 24. 未来真人语音摘要与 AI 发言

此部分只记录长期方案，当前阶段不实施。

真人在线下自由发言后，通过手机按住录制一段总结。流程必须是：

```text
录音
  -> 上传服务器
  -> 语音转文字
  -> 真人检查并修改文字
  -> 真人确认发布
  -> 生成 speech_published 领域事件
  -> 发送给所有有权接收的 AI 玩家
```

语音识别结果不能未经确认直接进入 AI 上下文。确认后的文字应同时展示给所有真人，保证真人和 AI 看到一致的公共记录。

未来还需要处理：

- HTTPS 下的浏览器麦克风权限。
- Android 与 iPhone 录音格式差异。
- 上传失败后的文字输入回退。
- 原始音频保留时间和隐私说明。
- 当前发言者和死亡玩家权限校验。
- 录音与发布命令幂等。
- 将玩家发言作为不可信模型输入，防止 Prompt 注入。
- AI 公开发言的文字展示和单一公共设备 TTS 播放，避免多台手机同时播放产生回声。

---

## 25. 下一次对话交接摘要

新对话开始时应首先阅读本文档，并以以下任务为当前目标：

> 在不破坏现有狼人杀完整流程和 71 项测试的前提下，拆出统一多游戏引擎边界，将狼人杀作为第一个注册引擎，并为标准三人斗地主接入准备房间人数、命令、状态和私人视图接口。

实施时优先检查：

1. `src/server.ts` 中的 `Room.game`、`playerGameView`、`actingPlayerIds` 和具体狼人杀事件处理。
2. `src/domain/game.ts` 中可迁入 `games/werewolf` 的状态和纯规则。
3. 房间当前全局 `MIN_PLAYERS`、`MAX_PLAYERS` 与具体游戏人数规则的耦合。
4. 正式页面默认直接进入狼人杀配置的入口流程。
5. 现有 71 项测试作为狼人杀和房间功能的回归基线。
6. 《多游戏平台下一阶段实施工作文档.md》中的阶段 1 目录、接口和验收要求。

本阶段不实现斗地主牌型、不移植血染钟楼算法，也不实现任何 AI 业务逻辑。多游戏引擎完成后，按“斗地主规则核心 → 斗地主多人闭环 → 持久化 → 血染钟楼”的顺序推进。
