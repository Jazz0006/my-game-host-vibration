# 当前开发路线与架构基线

> 项目：`Jazz0006/my-game-host-vibration`  
> 状态日期：2026-08-17  
> 用途：给后续开发、Codex 和架构审计提供“当前有效路线”的短基线。长期设计细节仍以 `长期架构与DurableObjects迁移设计_V3.md` 为主。

---

## 1. 当前有效设计基线

当前正式开发路线回到：

**`长期架构与DurableObjects迁移设计_V3.md`**

V3 的核心判断继续有效：

- 保留现有 `GameModule`，不以重写完整 Runtime 为目标；
- `WerewolfGameJudge` 主要作为 Cloudflare Durable Objects、WebSocket、Snapshot、Revision、commandId、Reconnect、多客户端等平台基础设施的参考；
- 不完整 Fork `WerewolfGameJudge`，也不依赖它提供本项目完整的狼人杀游戏流程；
- 当前优先把狼人杀自动主持体验做好，同时确保未来增加动态剧本、动态角色以及 BotC 时，不需要不断修改写死的平台流程。

### 关于 V4

`长期架构、规则引擎与 Durable Objects 迁移设计 V4` 是 2026-08-17 下午的一次探索性架构方案。

该探索曾考虑更直接地引入/借鉴 `WerewolfGameJudge` 的规则组织方式，并把当前工作进一步扩展为通用 RuleEffect / Resolver / RuleState 体系。

后续复核发现：

- 参考项目并不包含本项目需要的完整自动主持游戏流程；
- 继续沿该方向容易把当前目标从“动态游戏流程与角色扩展”扩大为“大型通用规则引擎重构”；
- 这不是当前阶段的首要问题。

因此：

> **V4 不再作为实施基线。若 V4 与本文件或 V3 冲突，以本文件 + V3 为准。**

V4 中已经验证且独立有价值的局部设计可以保留，但不能自动成为后续路线依据。

---

## 2. 当前真正要解决的架构问题

当前工作的核心不是证明“所有角色规则都能写成通用 DSL”。

真正需要证明的是：

> **新增剧本或角色时，正常情况下只需要修改/新增 Script、RoleDefinition 和对应角色逻辑，而不需要再修改写死的游戏进程。**

长期希望形成：

```text
Script / Board
      +
Role Registry / RoleDefinition
      +
Current Game State
      ↓
Night Orchestrator
      ↓
PendingInteraction
      ↓
玩家完成行动
      ↓
Orchestrator 决定下一条 Interaction
```

而不是：

```text
新增一个角色
      ↓
修改 Role union
修改 GamePhase union
增加 night_xxx
修改 NIGHT_ORDER
修改 nextNightPhase()
修改固定 switch / if
      ↓
角色才能运行
```

这也是未来动态剧本和 BotC 扩展是否成功的关键架构检验。

---

## 3. V3 PR B 的核心目标

V3 中 PR B（Night Interaction / Orchestrator）的长期验收含义是：

- 本局没有某角色，就不生成该角色 Interaction；
- 本局新增某个有行动的角色，Orchestrator 能根据角色/剧本定义发现它；
- 客户端根据 `PendingInteraction` 判断是否行动，而不是依赖固定 `GamePhase`；
- Night Orchestrator 最终能够根据 Script + RoleDefinition + state 决定当前/下一条实际行动；
- 固定 `night_guard / night_werewolf / night_witch / night_seer` 应逐步退化为 legacy 兼容层，而不是未来新增角色的必经路径。

不要求一次 PR 就删除所有 legacy phase。迁移应小步进行，并持续保证现有完整游戏流程不回归。

---

## 4. 当前已经完成的工作

### PR A — GameModule hardening ✅

已验证：

- `GameModule` 可独立测试；
- Player / Host / Public View 隐私边界；
- Host 本身是普通玩家，不自动获得秘密信息；
- 多回合基础行为有回归测试保护。

### PR B — PendingInteraction 基础 ✅

已建立 transport-neutral `PendingInteraction`，包含：

- `interactionId`；
- actor player IDs；
- single / group mode；
- wake policy；
- completion policy；
- 私有 actor 信息留在服务器/玩家视图边界内。

### PR B2 — Role Registry / Script Architecture ✅

已建立：

- `RoleDefinition`；
- Role Registry；
- ScriptDefinition；
- registry-driven interaction planner。

这一阶段证明了“谁需要行动”可以开始由角色定义驱动。

### PR B3 / B3.1 / B3.2 — Complex role hook / Hunter migration ✅

使用猎人验证：

- 角色触发逻辑可以离开 legacy domain 的 Hunter-specific fallback；
- production runtime 可以通过 Role Registry / Hook 路径触发猎人死亡后行动；
- legacy Hunter fallback 已删除；
- 生产规则测试走统一入口。

### PR B4a — Cupid relationship / chained-death rule spike ✅（架构局部验证）

当前 B4 分支已经验证：

- `WerewolfRuleState` 可保存 relationship；
- 丘比特可以选择两名恋人；
- 一名恋人死亡可产生另一名恋人的连锁死亡；
- death chain 可防止 A → B → A 无限循环；
- RuleEffect 可以区分玩家 interaction 和系统规则 death effect；
- 这些规则状态不需要继续污染 legacy `GameState`。

但必须明确：

> **B4a 只证明了“丘比特关系/殉情规则如何建模”，还没有完成最初最重要的“动态游戏流程”验证。**

当前 experimental Cupid 尚未正式进入 Script / Registry / Night Orchestrator，因此不能把 B4a 单独视为动态剧本架构已经通过。

---

## 5. 下一步：PR B4b — Dynamic Cupid Orchestration Spike

下一步优先级高于 Cupid 的人狼恋阵营/胜利规则，也高于继续扩展通用 RuleEffect vocabulary。

### 目标

使用丘比特验证：

> **一个“只在首夜行动的新角色”能否通过 Script + RoleDefinition 加入游戏，而不要求给 legacy 游戏流程增加新的固定 `night_cupid` 阶段。**

### 建议测试剧本

例如：

```text
Cupid
Guard
Werewolf
Witch
Seer
Villagers...
```

### 第一夜预期

Orchestrator 应根据角色定义/剧本产生类似：

```text
CupidLinkLovers
→ GuardProtect
→ WolfKill
→ WitchAction
→ SeerCheck
```

具体顺序由角色/Script metadata 定义，不由 `domain/game.ts` 新增固定 Cupid 分支决定。

### 第二夜及以后预期

丘比特是 only-first-night interaction，因此自动不再出现：

```text
GuardProtect
→ WolfKill
→ WitchAction
→ SeerCheck
```

无需在固定状态机中写：

```text
if (nightNumber > 1) skip cupid
```

角色自身/interaction eligibility metadata 应表达这一条件。

### B4b 核心验收条件

1. 新增 Cupid 到测试 Script 时，不修改 legacy `NIGHT_ORDER`；
2. 不新增 `GamePhase = "night_cupid"` 才能运行丘比特；
3. Orchestrator 能从 Script / Registry 发现 Cupid interaction；
4. Cupid 只在第一夜出现；
5. 第二夜自动跳过；
6. 没有 Cupid 的旧 Script 行为完全不变；
7. 客户端/PlayerView 只依赖 active `PendingInteraction` 即可知道丘比特玩家需要行动；
8. B4a relationship state 能接收该 interaction 的结果；
9. 全部现有回归测试继续通过。

### Architecture guard test

建议增加架构测试，至少锁定：

- experimental/新角色不能要求修改 `domain/game.ts` 的 `NIGHT_ORDER` 才能进入夜间编排；
- Night Planner / Orchestrator 的角色顺序来源是 Registry / Script metadata；
- 新 interaction kind 不要求在平台层增加 role-specific 判断。

---

## 6. B4b 之后的顺序

B4b 通过后，再决定下一步，而不是现在提前扩张规则引擎。

建议顺序：

### B4c（如确有需要）— Production integration

把动态 interaction orchestration 接进正式 `WerewolfGameModule` 流程，同时逐步减少 legacy phase 对新增角色的约束。

### 后续复杂角色验证

可根据实际收益选择：

- 丘比特人狼恋 alignment / victory；
- 机械狼：验证“身份与当前能力分离 / 动态能力”；
- 野孩子等角色：验证持久目标/动态阵营；
- 其他真正会给架构带来新能力要求的角色。

原则：

> **每增加一个复杂角色，优先把它当成架构压力测试，而不是为了尽快积累角色数量。**

---

## 7. 后续 Cloudflare / Durable Objects 路线保持 V3

动态游戏流程验证完成后，V3 的 Cloudflare 主路线仍然有效：

```text
PR C — Minimal Cloudflare GameRoom
PR D — Reconnect Hardening
PR E — Host Recovery Console
PR F — WeChat Thin Native Shell
```

Durable Object 只负责：

- Room；
- Session；
- authoritative state；
- command dispatch；
- Snapshot；
- Revision；
- commandId；
- WebSocket；
- reconnect。

它不应知道：

- 丘比特；
- 女巫；
- 猎人；
- BotC 投毒者；
- BotC 说书人推荐。

---

## 8. 当前决策原则

后续开发遇到路线选择时按以下优先级判断：

1. **先保证当前狼人杀完整游戏可用且不回归；**
2. **优先消除新增 Script / Role 时对固定游戏流程的依赖；**
3. `PendingInteraction` 是客户端和平台层理解“谁现在要行动”的主要合同；
4. Script 描述本局角色组合，不复制角色实现；
5. RoleDefinition 描述角色元数据、interaction 与必要的角色规则；
6. 只在真实复杂角色证明需要时增加通用抽象；
7. 不为了抽象完整而提前建设大型 DSL / Rule Engine；
8. `WerewolfGameJudge` 继续作为成熟 Cloud Platform 参考，不作为完整游戏引擎依赖；
9. V3 的 Durable Objects / reconnect / 多客户端计划继续有效；
10. 如果未来设计文档与本文件冲突，先重新确认产品目标，再修改基线，不让临时探索自动覆盖主路线。

---

## 9. 当前一句话状态

> **已完成 GameModule、PendingInteraction、Role Registry、Hunter hook 和 Cupid relationship/death-chain 的基础验证；下一步不是继续扩展通用规则引擎，而是用 Cupid 真正验证 Script + RoleDefinition 驱动的动态 Night Orchestrator，使新增角色不再依赖写死的游戏 phase/流程。**
