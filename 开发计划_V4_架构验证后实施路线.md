# 多游戏自动主持系统开发计划 V4：当前实施路线

> 项目：`Jazz0006/my-game-host-vibration`  
> 主分支：`main`  
> 状态日期：2026-08-19  
> 当前正式游戏：狼人杀  
> 长期目标：Cloudflare Workers + Durable Objects + Web / 微信小程序，多游戏扩展到 Blood on the Clocktower  
> 长期架构参考：`长期架构与DurableObjects迁移设计_V3.md`

---

## 1. 文档地位

本文件是**当前唯一的开发执行路线**。

开发时文档优先级：

1. `开发计划_V4_架构验证后实施路线.md` — 当前进度、下一步 PR、实施顺序；
2. `长期架构与DurableObjects迁移设计_V3.md` — 长期平台边界和 Durable Objects 方向；
3. `README.md` — 项目入口、运行和测试说明。

不要用长期架构文档反推当前实施顺序。C4 完成以前，不开始正式 Durable Objects 迁移。

---

## 2. 当前基线

截至 2026-08-19，已完成：

```text
PR #17  post-B5.1 Hunter target guard hotfix
PR #18  C1 Rejoin Identity Contract
PR #19  C2 Room Snapshot Contract
PR #20  C3 Idempotent Commands
C3.1    Explicit Socket Command Wiring + lifecycle hardening
PR #22  C4.1 Resend Current Action
PR #23  C4.2 Privacy-safe Host Recovery Status
PR #24  C4.3 Host-assisted Identity Recovery
```

C3.1 采用显式 transport `commandId`，没有采用已经关闭的 PR #21 middleware + AsyncLocalStorage 实验方案。

当前下一步：**C4.4 Stuck Interaction Recovery**。

---

## 3. 已确认的平台边界

已通过代码和测试验证：

- `GameModule` 作为平台与具体游戏契约；
- Player / Host / Public View 私密信息边界；
- transport-neutral `PendingInteraction`；
- Role Registry / Script / Night Planner；
- lifecycle hooks 与 Hunter；
- `WerewolfRuleState` sidecar；
- relationship / chained death；
- effective team 与 assigned role 分离；
- ability source / copied capability / consumable resource；
- ruleset variation；
- stable player identity / resume token；
- authoritative RoomSnapshot + revision；
- command receipt / idempotency；
- explicit Socket.IO commandId wiring。

继续保持：

```text
domain
  → generic contracts / hooks only

games/werewolf
  → concrete Werewolf rules

platform/runtime
  → room / identity / snapshot / command / connection
```

不为了 BotC 提前建设大型规则 DSL。

---

## 4. C1 — Reconnect Identity ✅

PR #18 已合并。

核心合同：

- `playerId` 是稳定身份，不等于 `socket.id`；
- 客户端保存 plaintext resume token；
- 服务端只保存 token hash；
- `player:resume` 使用 `roomId + playerId + resumeToken` 恢复；
- 新连接替换旧连接并发送 `session:replaced`；
- 不创建重复玩家。

**正常掉线恢复永远优先走 C1。** 手机切 App、熄屏、Wi-Fi 短断、页面刷新等，只要本机凭证仍在，都应自动恢复，不需要 Host 或恢复码。

---

## 5. C2 — Room Snapshot + Revision ✅

PR #19 已合并。

Authoritative snapshot 包含 room metadata、membership、gameConfig、game state、ruleState、pendingInteraction、revision，以及 C3 command receipts。

关键原则：

- socketId / connected 不进入持久化 snapshot；
- plaintext resume token 不进入 snapshot；
- private player view 在恢复后按 stable playerId 重建；
- V1 使用 authoritative snapshot，不使用 event replay。

---

## 6. C3 / C3.1 — Idempotent Commands ✅

PR #20 与 C3.1 已完成。

核心合同：

```text
Transport Command Envelope
  commandId
  actionId? / payload
        ↓
Runtime idempotency boundary
        ↓
WerewolfCommand / GameModule
```

必须保持：

```text
commandId = transport/runtime concern
actionId  = game interaction concurrency concern
```

客户端正常原则：

```text
new user action → new commandId
retry same action → same commandId
```

replayed command 不重复 broadcast、阶段推进、投票结算、发牌或其他 secondary side effects。

实验室在 2026-08-19 已同步 C3 契约：所有通过实验室 `emitAck()` 发出的游戏命令都会附带唯一 commandId，包含确认身份、批量确认、夜间行动和投票。

---

## 7. C4 — Host Recovery

房主是 **Recovery Controller**，不是真人法官。

禁止给 Host：

- 默认查看所有秘密身份；
- 默认查看秘密行动答案；
- 正常游戏中的上帝视角。

Host Recovery 权限和秘密 Host View 权限必须继续分离。

### C4.1 — Resend Current Action ✅

PR #22 已合并。

- Host 可重新提醒当前在线行动玩家；
- 复用 `player:action-alert`，带 `resumed: true`；
- 不改变 phase / actionId / PendingInteraction / game state；
- 同 commandId retry 不重复震动；
- 新 commandId 代表新的人工提醒意图。

### C4.2 — Privacy-safe Recovery Status ✅

PR #23 已合并。

Host 只看到聚合恢复状态：

```text
hasPendingInteraction
waitingCount
onlineWaitingCount
offlineWaitingCount
```

不暴露 actor id、姓名、座位、角色、行动目标或秘密答案。普通玩家 room view 不包含 recovery 字段。

### C4.3 — Host-assisted Identity Recovery ✅

PR #24 于 2026-08-19 完成本地验证；`npm run typecheck` 与完整 `npm test` 全部通过。

C4.3 只处理**长期恢复凭证已经丢失**的异常情况，例如：

- 换设备；
- 浏览器数据被清除；
- 换浏览器；
- 无痕/私密浏览数据消失。

正常 C1 resume 不需要 Host。

最终 C4.3 合同：

- Host 只能为**离线的其他玩家**生成 recovery code；
- Host 看不到玩家原来的长期 resume token；
- recovery code 为 **6 位纯数字**，方便现场口头传递和手机输入；
- recovery code 5 分钟有效，只能使用一次；
- 同一玩家重新生成时，旧 code 立即失效；
- 同一房间连续 5 次错误 recovery code 后，当前未使用 recovery grants 全部作废，需要 Host 重新生成；
- 玩家通过 `player:claim-identity-recovery` 恢复原 stable `playerId`；
- 成功后轮换长期 resume token，旧设备上的旧 token 立即失效；
- recovery grant 只存在于临时 runtime memory，不进入 RoomSnapshot / room state / command receipts；
- Host 自身不通过 C4.3 恢复，因为不存在第二个可信授权者。

相关 UX / 测试改进：

- 房间号从 6 位缩短为 **4 位数字（1000–9999）**；
- Web 普通加入、身份恢复、实验室统一使用 4 位契约；
- `/dev/lab` 增加“模拟掉线”和 C4.3 新虚拟客户端接管测试；
- 实验室确认身份 / 全部确认身份及后续玩家操作统一补齐 C3 commandId。

### C4.4 — Stuck Interaction Recovery ← 明天下一步

**2026-08-19 收工点：明天从这里继续。**

第一步不是直接加“跳过阶段”按钮，而是先审计当前 `PendingInteraction` 和所有生产 phase，列出真实会导致 interaction 卡死的故障类别，然后设计最小恢复动作。

C4.4 边界：

- 不替玩家提交秘密答案；
- 不让 Host 查看秘密行动答案；
- 不随意 skip / advance phase；
- 优先复用 `PendingInteraction`、`actionId`、stable playerId、private view regeneration；
- recovery command 必须可测试、可审计、幂等；
- 不扩大到 Durable Objects；
- 不同时实施 C4.5。

建议 C4.4 开始时先审计：

1. 当前哪些 interaction 会等待一个或多个 actor；
2. actor 断线后 C1/C4.3 恢复是否足够；
3. actor 在线但客户端 UI 丢失/未收到 alert 时 C4.1 是否足够；
4. 哪些情况仍可能留下 authoritative pending interaction 却无法完成；
5. 对这些剩余情况，最小安全恢复动作是什么。

### C4.5 — Abort / Return Lobby

C4.4 完成后再设计：终止当前游戏，以及在状态模型允许时安全回到 lobby。

---

## 8. 后续正式路线

```text
C1 Reconnect Identity ✅
    ↓
C2 Room Snapshot + Revision ✅
    ↓
C3 Idempotent Commands ✅
    ↓
C3.1 Explicit Socket Command Wiring ✅
    ↓
C4.1 Resend Current Action ✅
    ↓
C4.2 Recovery Status ✅
    ↓
C4.3 Identity Recovery ✅
    ↓
C4.4 Stuck Interaction Recovery ← 下一步
    ↓
C4.5 Abort / Return Lobby
    ↓
D1 Cloud Room abstraction
    ↓
D2 Cloudflare Worker + Durable Object skeleton
    ↓
D3 Durable Object persistence
    ↓
D4 Hibernation WebSocket
    ↓
D5 Node / Cloudflare parity tests
    ↓
E Multi-client boundary
    ↓
WeChat Mini Program shell
    ↓
BotC production expansion
```

---

## 9. Durable Objects 前置条件

进入 D 阶段前要求：

- [x] player identity 与 socket connection 分离；
- [x] resume token 能恢复同一 player；
- [x] snapshot 可 JSON round-trip；
- [x] ruleState 包含在恢复状态中；
- [x] active PendingInteraction 可恢复；
- [x] private view 可按 playerId 重建；
- [x] revision contract 已建立；
- [x] bounded command receipt / idempotency core 已建立；
- [x] 真实 Web / Socket transport 显式传递 commandId；
- [x] duplicate transport delivery 不重复 secondary side effects；
- [x] host 已具备提醒、诊断和身份协助恢复能力；
- [ ] stuck interaction recovery 完成；
- [ ] abort / return lobby recovery 完成；
- [ ] reconnect / retry / recovery 集成测试稳定。

---

## 10. D 阶段原则

### D1 Cloud Room abstraction

平台核心负责：membership、session identity、revision、command dedupe、snapshot、GameModule dispatch。

Node adapter 使用 Socket.IO；Cloudflare adapter 使用 Durable Object WebSocket。

Core 不应 import Express、Socket.IO、Cloudflare runtime types 或 Werewolf concrete roles。

### D2–D4

目标保持 V3：

```text
roomCode → Durable Object
one room = one authoritative DO
Durable Object Storage
Hibernation WebSocket
player-specific unicast view
public broadcast
```

V1 不引入 event sourcing、R2、复杂 Room Saga 或大型数据库 migration framework。

### D5 parity

核心验收：

```text
same snapshot + same command
→ same authoritative game result
```

---

## 11. Multi-client / 微信小程序原则

长期协议收敛为：

```text
Command Envelope
Snapshot / Player View
Realtime Event
Reconnect
```

Web、微信小程序、未来 Native App 都只是 client adapter。业务规则不复制到客户端。

震动和生命周期属于 capability adapter：

```text
Web              → navigator.vibrate
WeChat Mini App  → wx vibration + lifecycle
Future native    → native haptics
```

---

## 12. BotC 长期边界

Blood on the Clocktower 是明确目标，但应在 Room / Reconnect / Durable Object 基础设施稳定后生产化。

现有抽象可复用 PendingInteraction、Role Registry / Script、lifecycle hooks、sidecar ruleState、effective team、ability source / resource。

自动说书人 clue / recommendation 必须保持独立 recommendation layer，不进入 Room Runtime 平台核心。

---

## 13. 工程纪律

每个 PR 保持小范围：

1. 一个主要架构目标；
2. 先确认 contract；
3. 修改 production integration；
4. 增加回归 / 集成测试；
5. `npm run typecheck`；
6. `npm test`；
7. review；
8. squash merge；
9. 再进入下一项。

特别注意：

- 不让工具限制决定长期架构；
- 不为了 BotC 提前建设大型 DSL；
- 不在微信客户端复制规则；
- 不让 core import Cloudflare types；
- 不把 socketId 当稳定身份；
- 不把 Host 当真人法官；
- 不牺牲 PlayerView 私密性换取开发便利。

---
