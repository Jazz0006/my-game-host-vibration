# 多游戏自动主持系统开发计划 V4：架构验证后实施路线

> 项目：`Jazz0006/my-game-host-vibration`  
> 主分支：`main`  
> 日期：2026-08-18  
> 当前正式游戏：狼人杀  
> 长期目标：Cloudflare Workers + Durable Objects + Web / 微信小程序，多游戏扩展到 Blood on the Clocktower  
> 架构参考：`长期架构与DurableObjects迁移设计_V3.md`

---

# 1. 文档定位

本文件是经过 PR A ～ B5.1 实际实现和测试后的**最新开发执行计划**。

它不重复 V3 的全部长期架构设计，而是回答三个问题：

1. 已经通过代码和测试验证了什么；
2. 哪些架构探索应该停止，避免过度设计；
3. 下一阶段按什么顺序进入 Reconnect、Room Recovery、Cloud Room 和 Durable Objects。

从本文件开始，复杂角色不再作为近期主线。已经完成的角色 architecture spike 足以证明平台核心可以承载后续狼人杀角色和 BotC 扩展。

---

# 2. 当前主分支基线

PR #16 已于 2026-08-18 squash merge。

当前已合并基线：

```text
main
└── ee3b2f5d149aad2feae4a111145379e0b684b4ad
    PR B5.1: Mechanical Wolf copy policy and resources
```

注意：PR #16 合并后 Codex Review 新发现一个 P2 边界问题：

> 机械狼复制猎人后，如果死亡时场上已经没有其他存活目标，不应继续产生 `hunter_shot` interaction。

该问题已经修复并补测试，但**尚未完成本地验证和合并**：

```text
PR #17
Hotfix: suppress copied Hunter shot without living targets
branch: codex/post-b5-1-hunter-target-guard
head: 0c7bc14d18921168c1c71a3fae59a2b7480b63b7
status: Draft / needs npm run typecheck + npm test
```

因此下一次开发会话第一件事应先完成 PR #17。

---

# 3. 已完成的架构验证

## 3.1 PR A — GameModule hardening

状态：完成并合并。

验证内容：

- 完整游戏流程测试；
- Player / Host / Public View 边界；
- 私密信息不可泄漏；
- 多轮状态推进；
- `GameModule` 可以继续作为游戏平台与具体游戏之间的核心契约。

结论：

> 不再进行第二轮“大规模 Game Contract 重构”。

---

## 3.2 PR B — PendingInteraction

状态：完成并合并。

验证内容：

- 权威 interaction 保留完整 actor IDs；
- acting player 只看到安全投影；
- 非行动玩家、房主、公共视图看不到秘密 interaction 元数据；
- 房主是玩家，不是拥有上帝视角的真人法官。

结论：

> `PendingInteraction` 已经是夜间主持、震动唤醒、未来 BotC 私密行动的正确抽象。

---

## 3.3 PR B2 — Role Registry / Night Planner

状态：完成并合并。

验证内容：

- 角色元数据集中注册；
- 夜间顺序可以由 metadata 驱动；
- 普通主动角色可以声明式表达；
- `single / group / explicit_confirmation` 等 interaction 模式可复用。

结论：

> 普通角色不需要 if/else 硬编码到平台层。

---

## 3.4 PR B3 / B3.1 / B3.2 — Lifecycle Hook 与生产 Hunter

状态：完成并合并。

验证内容：

- `beforeDeath`；
- `afterDeath`；
- `resolveTeam`；
- `evaluateVictory`；
- Hunter 生产逻辑通过 registry-backed runtime hooks 运行；
- domain 只保留 generic hook seam；
- 删除 legacy Hunter fallback。

重要架构边界：

```text
domain
  → generic runtime hooks only

games/werewolf
  → concrete role rules
```

结论：

> 游戏规则可以扩展，但 domain 不需要知道具体角色。

---

## 3.5 PR B4 — Cupid relationship + chained death

状态：完成并合并。

验证内容：

- `WerewolfRuleState` sidecar；
- 可序列化 Lovers relationship；
- 丘比特可以选择自己；
- 恋人连锁死亡；
- A → B → A 循环抑制；
- typed rule effects：interaction / death。

结论：

> 跨玩家关系不需要塞进 `GameState` 的大量角色专用字段。

---

## 3.6 PR B4.1 — 动态阵营与特殊胜利

状态：完成并合并。

验证内容：

- 角色身份和 effective team 分离；
- mixed Lovers 特殊胜利；
- 普通狼人 parity victory 可以被关系规则覆盖；
- 支持动态基础阵营 resolver；
- 支持两套丘比特规则：

```ts
"classic_millers_hollow"
"china_three_party"
```

中国三人第三方变体中：

```text
Cupid + Wolf Lover + Village Lover
→ lovers special team
```

同阵营恋人不自动变第三方。

结论：

> 平台应允许 ruleset variation，不应把某个平台玩法写成唯一规则。

---

## 3.7 PR B5 — Mechanical Wolf identity / ability-source separation

状态：完成并合并。

验证内容：

```text
assigned role
≠ effective team
≠ ability source
≠ perceived/check role
```

机械狼学习预言家后：

```text
assigned role     = mechanical_wolf
effective team    = wolf
ability source    = seer
perceived role    = seer
```

同时验证：

- `abilitySources` 可序列化；
- 学习只允许一次；
- 被学习玩家死亡后能力仍保留；
- copied interaction 不复制 source role 的 `phase / nightOrder`；
- 默认可支持“下一晚生效”。

结论：

> 身份和能力必须分离，不能靠修改 `game.roles[playerId]` 模拟复制角色。

---

## 3.8 PR B5.1 — Copy Policy + ability resources

状态：已合并，后续有 PR #17 小型 hotfix 待验证。

验证内容：

- 不复制整个 `RoleDefinition` / hook bundle；
- 显式 copy capability；
- copied Hunter = self-death interaction；
- copied Witch = 独立的一次性毒药；
- `abilityResources` 可序列化；
- copied resource 不共享 legacy `GameState.witchPoisonSpent`；
- activation night 不能被 consume API 绕过。

结论：

> 对复杂复制角色，应该显式声明“允许复制什么”，而不是建立无限扩展的通用规则 DSL。

---

# 4. 综合测试结论

整个 A ～ B5.1 阶段不是纯文档设计，而是经过连续实现、typecheck 和测试验证。

已确认的事实：

- B3.2 时完整测试基线为 24 test files / 144 tests 全部通过；
- 此后的 B4、B4.1、B5、B5.1 都在各自最新修改后由本地 `npm run typecheck` 与 `npm test` 验证通过；
- B5 曾发现 `exactOptionalPropertyTypes` 类型错误，并在测试前修复；
- B5.1 合并后自动 Code Review 又发现“Hunter 无合法目标仍触发”的遗漏，说明复杂规则边界需要继续保持“测试 + Review”双保险。

当前测试策略应继续保持：

```bash
npm run typecheck
npm test
```

每个 PR 必须在合并前执行。

对于复杂状态规则，新增行为必须配最小回归测试，不接受只改实现不补测试。

---

# 5. 现在应该停止什么

## 5.1 停止继续做角色 architecture spike

目前已经覆盖：

```text
Hunter
  → 单角色死亡 trigger

Cupid
  → setup + relationship + linked death

Lovers
  → dynamic alignment + special victory

Mechanical Wolf
  → identity / ability separation
  → copied passive trigger
  → consumable resource
```

这组角色已经覆盖未来复杂规则最重要的结构类型。

近期不要继续为了“证明架构”去实现：

- 更多机械狼特殊 case；
- 大量角色 copy policy；
- 通用规则 DSL；
- 通用 effect language；
- 全部狼人杀扩展角色；
- BotC 全角色抽象。

这些应该等真实产品需求出现时逐个接入。

---

## 5.2 不要提前 productionize Mechanical Wolf / Cupid

当前 experimental 代码的价值主要是证明边界。

除非下一版实际剧本需要这些角色，否则不应马上把它们全部加入：

- production role registry；
- night phase；
- UI；
- command handler；
- production settlement。

优先级已经转向“手机真实使用可靠性”。

---

# 6. 下一阶段总路线

从现在开始，主线调整为：

```text
PR #17 hotfix
    ↓
C1 Reconnect Identity
    ↓
C2 Room Snapshot + Revision
    ↓
C3 Idempotent Commands
    ↓
C4 Host Recovery
    ↓
D1 Cloud Room abstraction
    ↓
D2 Cloudflare Worker + Durable Object skeleton
    ↓
D3 Durable Object persistence
    ↓
D4 Hibernation WebSocket
    ↓
D5 Node / Cloudflare parity tests
    ↓
E Multi-client boundary
    ↓
WeChat Mini Program shell
    ↓
BotC production expansion
```

其中 C 阶段完成以前，不建议开始正式 Durable Objects 迁移。

原因：

> Durable Objects 最擅长承载“一个房间一个权威状态”，但必须先明确到底哪些状态需要恢复、客户端如何重新认领身份、重复 command 如何处理。否则只是把当前 Node 状态直接搬到云端，并没有解决真正的可靠性问题。

---

# 7. C 阶段：Reconnect / Room Recovery

这是下一阶段最高优先级。

实际手机场景最常见的断线来源：

- iPhone / Android 熄屏；
- 切换 App；
- 浏览器进入后台；
- Wi-Fi ↔ 蜂窝网络切换；
- WebSocket 被系统暂停；
- 页面重新加载。

这些应被视为**正常生命周期**，而不是异常事故。

---

## 7.1 C1 — Stable Player Identity / Rejoin Token

目标：

> socket connection 不再等于 player identity。

建议建立：

```ts
RoomMember {
  playerId: string
  rejoinToken: string
  displayName: string
  isHost: boolean
}
```

客户端本地保存：

```text
roomCode
playerId
rejoinToken
```

重新连接时：

```text
join room
  ↓
提供 rejoinToken
  ↓
服务器恢复原 playerId
  ↓
绑定新 socket
```

禁止依赖：

```text
socket.id == player identity
```

### C1 测试要求

- socket 更换后 playerId 不变；
- 错误 token 不能认领身份；
- 同一玩家快速重复连接有明确策略；
- host 断线重连后仍保持 host 身份；
- 游戏中重连不能重新抽角色。

---

## 7.2 C2 — Authoritative Snapshot + Revision

服务端维护：

```ts
RoomSnapshot {
  revision: number
  room
  membership
  game
  ruleState
  pendingInteraction
}
```

注意 private view 仍由服务端根据 playerId 重新生成，客户端不能获得 raw authoritative state。

恢复流程：

```text
Reconnect
  ↓
Authenticate rejoin token
  ↓
server reads authoritative RoomSnapshot
  ↓
GameModule.getPlayerView(playerId)
  ↓
private active interaction projection
  ↓
client replaces local state
```

不要做 event replay 作为 V1 恢复机制。

### C2 测试要求

至少覆盖断线发生在：

- role reveal；
- 狼人行动中；
- 女巫行动中；
- 白天投票中；
- Hunter trigger 中；
- 已产生 ruleState relationships / ability resources 的状态。

---

## 7.3 C3 — commandId / Idempotency

手机网络恢复后，客户端可能不知道最后一次 command 是否成功。

例如：

```text
玩家点击“确认查验”
服务器已处理
↓
连接瞬间断开
↓
客户端重新发送
```

如果没有 idempotency，会执行两次。

因此所有 mutation command 应有：

```ts
commandId: string
```

Room Runtime 保存近期已执行 command IDs / result reference。

目标：

```text
same commandId
→ at most once mutation
```

V1 不需要永久保存全部历史，只需要适合房间生命周期的 bounded dedupe。

---

## 7.4 C4 — Host Recovery

房主不是法官，但必须能处理异常。

建议最小权限：

- 查看谁在线 / 离线；
- 重新发送当前行动提醒；
- 允许某玩家重新认领身份；
- 跳过/取消卡死 interaction（需审计确认）；
- 终止游戏；
- 回到房间等待页。

不要给 host：

- 自动查看所有秘密身份；
- 查看所有秘密行动答案；
- 正常游戏中的上帝视角。

Host Recovery 应独立于 Host View 的秘密权限。

---

# 8. D 阶段：Cloud Room / Durable Objects

C 阶段稳定后才进入。

---

## 8.1 D1 — Cloud Room abstraction

先在 Node 环境定义平台运行边界，例如：

```text
RoomCore
  - membership
  - session identity
  - revision
  - command dedupe
  - snapshot
  - GameModule dispatch
```

Node runtime：

```text
Socket.IO adapter
```

Cloudflare runtime：

```text
Durable Object WebSocket adapter
```

RoomCore 不得 import：

- Express；
- Socket.IO；
- Cloudflare API；
- Werewolf concrete roles。

---

## 8.2 D2 — Worker + Durable Object skeleton

创建：

```text
src/runtime/cloudflare/
├── worker.ts
├── GameRoom.ts
├── RoomRepository.ts
└── RoomDirectory.ts
```

第一阶段只需要：

```text
roomCode → Durable Object
health
create room
join room
read snapshot
```

先不迁移完整游戏 mutation。

---

## 8.3 D3 — Durable Object persistence

一个房间一个 DO。

持久化：

- room metadata；
- membership identity；
- revision；
- game state；
- rule sidecar state；
- command dedupe state；
- current interaction。

V1 不引入 D1/R2/Event Sourcing。

---

## 8.4 D4 — Hibernation WebSocket

直接采用 Cloudflare Hibernation API，而不是先做长期驻留 socket 版本。

目标：

- 每个连接带 player/user tag；
- broadcast public changes；
- unicast private view；
- hibernate 后恢复连接上下文；
- socket 生命周期与 player identity 分离。

---

## 8.5 D5 — Runtime parity tests

同一个规则测试 fixture 必须可以在：

```text
Node Room Runtime
Cloudflare Room Runtime
```

得到相同 authoritative result / player view。

重点不是测试 WebSocket library 相同，而是测试：

```text
same command
+ same snapshot
→ same game result
```

---

# 9. E 阶段：Multi-client / 微信小程序

在 Cloud Room contract 稳定之后再开始。

服务端协议应该只有：

```text
Command
Snapshot/View
Realtime Event
Reconnect
```

Web 和微信小程序都只是不同 client adapter。

微信小程序重点利用：

- 原生震动；
- 生命周期事件；
- 后台/前台恢复；
- WebSocket reconnect；
- 必要时 WebView 承载复杂 UI。

不要把业务规则复制到小程序端。

---

# 10. BotC 进入时机

Blood on the Clocktower 仍然是明确的长期目标，但不应抢在 room/reconnect 基础设施之前。

现在已经验证的抽象对 BotC 有直接价值：

- `PendingInteraction` → 夜间私密行动；
- role registry → JSON/script role composition；
- lifecycle hooks → 死亡/状态触发；
- relationship / sidecar ruleState → reminder / relationship / dynamic effect；
- effective team → alignment change；
- ability source/resource → drunk/poison/borrowed/dynamic capabilities 的建模参考。

BotC 下一步生产化时，应优先做：

1. ScriptDefinition / JSON 剧本加载；
2. 角色元数据；
3. Night interaction planner；
4. Player-specific information generation；
5. 自动说书人 clue/recommendation 作为独立 recommendation layer。

不要把“自动说书人推荐算法”塞进 Room Runtime 或 GameModule 平台核心。

---

# 11. 下一轮 PR 建议

## PR C0 — 完成 post-B5.1 hotfix

当前：PR #17。

动作：

```bash
npm run typecheck
npm test
```

通过后：

- Ready for review；
- 检查 Codex review；
- squash merge。

---

## PR C1 — Rejoin Identity Contract

目标：建立稳定 player identity 与 rejoin token，彻底解除 socketId/playerId 耦合。

尽量只做：

- 类型；
- room member identity；
- token issue/validate；
- reconnect unit tests。

暂时不改 Cloudflare。

---

## PR C2 — Room Snapshot Contract

目标：明确一个房间恢复所需的 authoritative data。

包括：

- room metadata；
- membership；
- GameModule state；
- WerewolfRuleState；
- PendingInteraction；
- revision。

要求 JSON round-trip tests。

---

## PR C3 — Reconnect Flow

目标：真正模拟：

```text
connect → play → disconnect → new socket → rejoin → restore view
```

至少覆盖夜间行动中的恢复。

---

## PR C4 — commandId Idempotency

目标：重复发送命令不重复执行 mutation。

---

## PR C5 — Host Recovery Minimum Controls

目标：异常恢复，不增加日常上帝视角。

完成 C1-C5 后，再重新审计是否已经满足迁移 Cloud Room 的前置条件。

---

# 12. Definition of Ready：进入 Durable Objects 前必须满足

以下条件全部满足后，才进入 D 阶段：

- [ ] player identity 与 socket connection 分离；
- [ ] rejoin token 可恢复同一玩家；
- [ ] snapshot 可 JSON round-trip；
- [ ] ruleState 包含在恢复状态中；
- [ ] active PendingInteraction 可恢复；
- [ ] private view 重建无泄密；
- [ ] revision 已建立；
- [ ] commandId dedupe 已建立；
- [ ] host 可处理最小异常恢复；
- [ ] 断线/重连集成测试稳定。

---

# 13. 工程纪律

继续保持现有小 PR 工作流。

每个 PR：

1. 单一架构目标；
2. 先测试 contract，再扩大 production integration；
3. `npm run typecheck`；
4. `npm test`；
5. PR review；
6. squash merge；
7. 再进入下一项。

特别注意：

- 不因未来 BotC 就提前建立大型规则 DSL；
- 不因未来微信客户端就在客户端复制规则；
- 不因未来 Cloudflare 就让 core import Cloudflare types；
- 不把 socketId 写入 GameModule context；
- 不把 host 当真人法官；
- 不牺牲 PlayerView 私密性换开发方便。

---

# 14. 新对话继续开发的起点

在新的 ChatGPT / Codex 会话中，可以直接使用下面信息作为起点：

```text
Repository:
Jazz0006/my-game-host-vibration

Primary roadmap:
开发计划_V4_架构验证后实施路线.md

Long-term architecture reference:
长期架构与DurableObjects迁移设计_V3.md

main baseline after B5.1:
ee3b2f5d149aad2feae4a111145379e0b684b4ad

Immediate open PR:
#17 Hotfix: suppress copied Hunter shot without living targets
branch: codex/post-b5-1-hunter-target-guard
head: 0c7bc14d18921168c1c71a3fae59a2b7480b63b7

First action:
1. checkout PR #17 branch
2. npm run typecheck
3. npm test
4. review and squash merge if green

Next feature PR:
C1 Rejoin Identity Contract
```

完成 PR #17 后，不再继续角色 architecture spike；正式进入 Reconnect / Room Recovery 主线。

---

# 15. 当前阶段总结

A ～ B5.1 已经回答了最初最重要的架构问题：

> **现有 GameModule + Interaction + narrow lifecycle hooks + serializable sidecar rule state，足以承载从普通狼人杀角色到丘比特、恋人、机械狼等复杂规则，不需要推倒重来。**

现在项目最大的技术风险已经从“规则架构是否能扩展”转移为：

> **真实手机环境下，房间状态、玩家身份、秘密 interaction 能否在断线和恢复过程中保持正确。**

因此下一阶段的成功标准不再是“能支持更复杂的角色”，而是：

> **玩家熄屏、切 App、掉线、重连之后，可以在几秒内无感恢复到正确身份、正确私密视图和正确当前行动，并且不会重复执行任何游戏命令。**
