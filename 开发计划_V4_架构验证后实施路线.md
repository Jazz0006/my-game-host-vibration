# 多游戏自动主持系统开发计划 V4：当前实施路线

> 项目：`Jazz0006/my-game-host-vibration`  
> 主分支：`main`  
> 状态日期：2026-08-18  
> 当前正式游戏：狼人杀  
> 长期目标：Cloudflare Workers + Durable Objects + Web / 微信小程序，多游戏扩展到 Blood on the Clocktower  
> 长期架构参考：`长期架构与DurableObjects迁移设计_V3.md`

---

## 1. 文档地位

本文件是**当前唯一的开发执行路线**。

开发和 Codex 工作时，文档优先级为：

1. `开发计划_V4_架构验证后实施路线.md` — 当前进度、下一步 PR、实施顺序；
2. `长期架构与DurableObjects迁移设计_V3.md` — 长期平台边界和 Durable Objects 方向；
3. `README.md` — 项目入口、运行和测试说明。

已完成的角色 architecture spike、旧 MVP 设计和被新版取代的探索性架构文档不再保留在当前工作树中。历史需要时从 Git history 查看，不应作为新的实施依据。

---

## 2. 当前主分支基线

截至 2026-08-18：

```text
main
└── 2148c18a42a6ce6107b6d713bcc48be37ae86bea
    PR #20: C3 Idempotent Commands
```

已完成并合并：

```text
PR #17  post-B5.1 Hunter target guard hotfix
PR #18  C1 Rejoin Identity Contract
PR #19  C2 Room Snapshot Contract
PR #20  C3 Idempotent Commands
```

PR #21 `C3.1: Socket Command Wiring` 的 middleware + AsyncLocalStorage 实验方案已经**关闭且未合并**。远端分支 `agent/c3-1-socket-command-wiring` 已重置到上述 main 基线，因此该实验实现不属于当前代码。

下一步是在本地工作区重新实现一个显式 transport 版本的 C3.1。

---

## 3. 已完成的架构验证

A ～ B5.1 阶段已经通过代码和测试验证以下平台能力：

- `GameModule` 作为平台与具体游戏的契约；
- Player / Host / Public View 的私密信息边界；
- transport-neutral `PendingInteraction`；
- Role Registry / Script / Night Planner；
- generic lifecycle hooks 与生产 Hunter；
- `WerewolfRuleState` sidecar；
- relationship / chained death；
- effective team 与 assigned role 分离；
- ability source / copied capability / consumable resource；
- ruleset variation。

结论保持不变：

> 不再继续为了证明架构而增加复杂角色，也不建设大型通用规则 DSL。后续角色能力在真实产品需求出现时逐个接入。

重要边界：

```text
domain
  → generic contracts / hooks only

games/werewolf
  → concrete Werewolf rules

platform/runtime
  → room / identity / snapshot / command / connection
```

---

## 4. C1 — Reconnect Identity ✅

PR #18 已合并。

核心结果：

- `playerId` 是稳定身份，不等于 `socket.id`；
- 客户端持有 plaintext resume token；
- 服务端只保存 SHA-256 token hash；
- `player:resume` 使用 `roomId + playerId + resumeToken` 认领身份；
- 新连接替换旧连接并发送 `session:replaced`；
- host 重连后仍是 host；
- 不创建重复玩家；
- 旧 socket 随后 disconnect 不会把新连接错误标记为离线。

这一合同是后续手机熄屏、切 App、网络切换和微信生命周期恢复的基础。

---

## 5. C2 — Room Snapshot + Revision ✅

PR #19 已合并。

当前 authoritative snapshot 包含：

```text
revision
room metadata
membership
gameConfig
game state
ruleState
pendingInteraction
commandReceipts (C3 扩展)
```

关键原则：

- socketId / connected 等运行时连接字段不进入持久化 snapshot；
- plaintext resume token 不进入 snapshot；
- private player view 在恢复后由服务器根据稳定 playerId 重新生成；
- V1 recovery 使用 authoritative snapshot，不使用 event replay；
- revision 是非负 safe integer。

已覆盖 role reveal、狼人行动、女巫行动、白天投票、Hunter trigger，以及 relationships / ability sources / resources 的 JSON round-trip 恢复。

---

## 6. C3 — Idempotent Commands ✅

PR #20 已合并。

核心实现：

- platform-neutral `IdempotentCommandLedger`；
- bounded recent receipt history，默认 128 条；
- completed retry 返回原结果，不再次 mutation；
- concurrent retry 共用同一个 in-flight mutation；
- mutation 失败不保存 receipt，后续可以正常重试；
- receipt 随 C2 RoomSnapshot 持久化/恢复；
- player command key 由稳定 playerId scope；
- host command 使用 host scope；
- `commandId` 不进入游戏专用 `WerewolfCommand`。

当前核心合同：

```text
Transport Command Envelope
  commandId
  command payload
        ↓
Runtime idempotency boundary
        ↓
WerewolfCommand / GameModule
```

需要继续保持：

```text
commandId = transport/runtime concern
actionId  = game interaction concurrency concern
```

二者不能合并成同一个概念。

---

## 7. 当前下一步 — C3.1 Explicit Socket Command Wiring

### 7.1 为什么需要 C3.1

C3 已经有完整 ledger、snapshot 和 runtime facade，但现有 Web / Socket.IO transport 仍直接调用同步的：

```ts
runPlayerCommand(...)
runHostCommand(...)
```

真实客户端尚未把 `commandId` 显式带到服务器。

因此 C3.1 的目标是：

> **把已经验证的 C3 idempotency contract 正式接到真实 Web → Socket.IO → runtime 调用链。**

### 7.2 必须采用显式设计

目标调用链：

```text
Web client
  ↓
{ commandId, actionId?, payload... }
  ↓
Socket.IO handler
  ↓
runPlayerCommandIdempotent(...)
或 runHostCommandIdempotent(...)
  ↓
IdempotentCommandLedger
  ↓
WerewolfGameModule
```

不要采用以下长期方案：

- `AsyncLocalStorage` 隐式传递 commandId；
- Socket middleware 在 handler 外偷偷注入 command context；
- 根据 `actionId + event + payload` 长期推导伪 commandId；
- 用 TTL cache 猜测两个 host 操作是否是同一次 command；
- Node 专用机制成为 command contract 的必要组成部分。

这些可以作为迁移实验，但不是正式多客户端架构。

### 7.3 客户端要求

建议在 `public/app.js` 集中实现 command transport helper，而不是在每个按钮重复生成协议字段。

每一次**用户意图**生成一个新的 commandId，例如：

```js
crypto.randomUUID()
```

同一次操作发生网络 timeout / reconnect retry 时，必须复用原 commandId；新的用户操作必须生成新的 commandId。

因此不要简单地“每次 emit 都生成新 ID”。应区分：

```text
new user action
→ new commandId

retry same action
→ same commandId
```

客户端 helper 应继续复用当前 ack / error 处理，而不把游戏规则放到客户端。

### 7.4 服务端要求

需要直接修改 `src/server.ts` 的游戏 mutation handlers，使其显式接收 `commandId` 并调用：

```ts
runPlayerCommandIdempotent(...)
runHostCommandIdempotent(...)
```

至少覆盖当前真实 Werewolf mutation：

- `player:confirm-role`；
- `player:submit-wolf-target`；
- `player:submit-witch-action`；
- `player:submit-seer-target`；
- `player:confirm-seer-result`；
- `player:submit-guard-target`；
- `player:submit-hunter-execution`；
- `player:submit-vote`；
- `host:start-night`；
- `host:close-voting`；
- `host:begin-night-start`。

是否把 lobby 管理类 mutation（改名、换座、移除玩家等）纳入同一 command envelope，可以在 C3.1 审计时决定；不要因此扩大本 PR 范围到房间持久化设计。

### 7.5 replayed side-effect 要求

C3 ledger 保证的是 mutation 不重复，但 Socket handler 还有 broadcast、自动推进、`game:over`、`afterNightAction` 等副作用。

因此 handler 必须正确使用：

```ts
{ outcome, replayed }
```

重复 delivery 应：

- 向重试客户端返回成功 ack；
- 不再次执行 domain mutation；
- 不重复触发只应发生一次的 secondary server side effects；
- 必要的状态同步可以安全重发，但不得造成第二次阶段推进、第二次结算或第二次自动 command。

这一点必须有集成测试，而不能只依赖 ledger unit test。

### 7.6 C3.1 测试要求

至少增加真实 Socket.IO 测试：

1. 同一 `commandId` 的 `player:confirm-role` 重发只 mutation 一次；
2. 同一 actor + 同一 commandId 返回成功 replay；
3. 不同 commandId 被视为新 command；
4. 两个 player 使用相同 raw commandId 不互相冲突；
5. ACK 丢失后重发同一 commandId 不重复推进 phase/actionId；
6. snapshot/recovery 后同 commandId 仍 dedupe；
7. replay 不重复关键 secondary side effect；
8. 缺少/空 commandId 的正式 mutation 请求有明确失败策略。

完成后运行：

```bash
npm run typecheck
npm test
```

### 7.7 本地开发建议

C3.1 应在本地工作区 / Codex 直接对大文件做局部 patch。文件大不是架构理由；Git 最终只记录实际 diff。

远端分支：

```text
agent/c3-1-socket-command-wiring
```

当前已重置到：

```text
2148c18a42a6ce6107b6d713bcc48be37ae86bea
```

因此可以从干净的 post-C3 基线重新实现。

---

## 8. C4 — Host Recovery

C3.1 合并后进入 C4。

房主不是真人法官，而是 Recovery Controller。

最小目标：

- 查看在线 / 离线玩家；
- 重新发送当前行动提醒；
- 帮助玩家恢复/重新认领身份时提供安全入口；
- 对卡死 interaction 提供最小恢复动作；
- 终止当前游戏；
- 回到房间等待状态（如果状态模型允许且经过测试）。

不要给 host：

- 默认查看所有秘密身份；
- 默认查看秘密行动答案；
- 正常游戏中的上帝视角。

Host Recovery 权限与 Host View 的秘密信息权限必须继续分离。

---

## 9. 后续正式路线

```text
C3.1 Explicit Socket Command Wiring
    ↓
C4 Host Recovery
    ↓
D1 Cloud Room abstraction
    ↓
D2 Cloudflare Worker + Durable Object skeleton
    ↓
D3 Durable Object persistence
    ↓
D4 Hibernation WebSocket
    ↓
D5 Node / Cloudflare parity tests
    ↓
E Multi-client boundary
    ↓
WeChat Mini Program shell
    ↓
BotC production expansion
```

C4 完成以前，不开始正式 Durable Objects 迁移。

---

## 10. Durable Objects 前置条件

进入 D 阶段前要求：

- [x] player identity 与 socket connection 分离；
- [x] resume token 能恢复同一 player；
- [x] snapshot 可 JSON round-trip；
- [x] ruleState 包含在恢复状态中；
- [x] active PendingInteraction 可恢复；
- [x] private view 可按 playerId 重建；
- [x] revision contract 已建立；
- [x] bounded command receipt / idempotency core 已建立；
- [ ] 真实 Web / Socket transport 显式传递 commandId；
- [ ] duplicate transport delivery 不重复 secondary side effects；
- [ ] host 有最小异常恢复能力；
- [ ] reconnect / retry / recovery 集成测试稳定。

---

## 11. D 阶段原则

### D1 Cloud Room abstraction

平台核心负责：

```text
membership
session identity
revision
command dedupe
snapshot
GameModule dispatch
```

Node adapter 使用 Socket.IO；Cloudflare adapter 使用 Durable Object WebSocket。

Core 不应 import：

- Express；
- Socket.IO；
- Cloudflare runtime types；
- Werewolf concrete roles。

### D2–D4

目标保持 V3：

```text
roomCode → Durable Object
one room = one authoritative DO
Durable Object Storage
Hibernation WebSocket
player-specific unicast view
public broadcast
```

V1 不引入 event sourcing、R2、复杂 Room Saga 或大型数据库 migration framework。

### D5 parity

核心验收：

```text
same snapshot + same command
→ same authoritative game result
```

Node 与 Cloudflare 的 WebSocket library 不需要相同，但游戏结果和私密 view contract 必须一致。

---

## 12. Multi-client / 微信小程序原则

服务端长期协议收敛为：

```text
Command Envelope
Snapshot / Player View
Realtime Event
Reconnect
```

Web、微信小程序、未来 Native App 都只是 client adapter。

业务规则不复制到客户端。

震动和生命周期属于 capability adapter，例如：

```text
Web              → navigator.vibrate
WeChat Mini App  → wx vibration + lifecycle
Future native    → native haptics
```

这也是 C3.1 必须坚持显式 command envelope、避免 Node 隐式 context 的原因。

---

## 13. BotC 长期边界

Blood on the Clocktower 是明确目标，但应在 Room / Reconnect / Durable Object 基础设施稳定后生产化。

现有抽象可以复用：

- `PendingInteraction` → 夜间私密行动；
- Role Registry / Script → 剧本和角色组合；
- lifecycle hooks → 死亡/状态触发；
- sidecar ruleState → relationship / reminder / dynamic effects；
- effective team → alignment change；
- ability source / resource → drunk、poison、borrowed/dynamic capability 的建模参考。

自动说书人 clue / recommendation 必须保持独立 recommendation layer，不进入 Room Runtime 平台核心。

---

## 14. 工程纪律

每个 PR 保持小范围：

1. 一个主要架构目标；
2. 先确认 contract；
3. 修改 production integration；
4. 增加回归 / 集成测试；
5. `npm run typecheck`；
6. `npm test`；
7. review；
8. squash merge；
9. 再进入下一项。

特别注意：

- 不让工具限制决定长期架构；
- 大文件应通过本地/Codex局部 patch 修改，而不是增加隐式 workaround；
- 不为了 BotC 提前建设大型 DSL；
- 不在微信客户端复制规则；
- 不让 core import Cloudflare types；
- 不把 socketId 当稳定身份；
- 不把 host 当真人法官；
- 不牺牲 PlayerView 私密性换取开发便利。

---

## 15. 新会话 / Codex 起点

```text
Repository:
Jazz0006/my-game-host-vibration

Canonical execution roadmap:
开发计划_V4_架构验证后实施路线.md

Long-term architecture reference:
长期架构与DurableObjects迁移设计_V3.md

Current main baseline:
2148c18a42a6ce6107b6d713bcc48be37ae86bea
PR #20 C3 Idempotent Commands merged

Next work:
C3.1 Explicit Socket Command Wiring
branch: agent/c3-1-socket-command-wiring

Implementation rule:
Modify public/app.js and src/server.ts directly with local/Codex partial patches.
Use explicit commandId in the transport envelope.
Call runPlayerCommandIdempotent / runHostCommandIdempotent explicitly.
Do not use the closed PR #21 AsyncLocalStorage/middleware experiment.

Validation:
npm run typecheck
npm test

After C3.1:
C4 Host Recovery
```

---

## 16. 当前一句话状态

> **角色架构验证已经结束；C1 稳定身份、C2 authoritative snapshot、C3 bounded idempotency core 已全部完成并合并。当前唯一下一步是在本地完成 C3.1 显式 Socket command envelope 接线，之后进入 C4 Host Recovery，再开始 Durable Objects 迁移。**
